# PREVIEW ONLY — REMOVE THE DISALLOW BEFORE PUBLIC LAUNCH
# While the phone, hours, maps link, and some other details are still
# placeholders, this keeps the site out of Google. Indexing wrong
# information is easy to do and slow to undo.
#
# ON LAUNCH DAY: delete the "Disallow: /" line under User-agent: *.
# That is the only remaining step — the noindex meta tags are already off
# the four indexable pages, and the sitemap is declared below.
#
# scripts/check-seo.mjs enforces this: while LAUNCHED=false it accepts the
# Disallow as intentional; set LAUNCHED=true and it fails CI if anything
# ever blocks Googlebot again.

User-agent: *
Disallow: /

Sitemap: https://me-kitchen.com/sitemap.xml

# Semrush's Site Audit bot is allowed through deliberately — a technical
# crawl (broken links, missing tags, headers) is useful pre-launch and is
# not the same as being indexed by a search engine. This does not affect
# the noindex meta tags, which stay in place regardless. Semrush's own
# setup instructions specify this record at the end of the file.
User-agent: SiteAuditBot
Allow: /
